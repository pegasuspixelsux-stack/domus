/**
 * Shared user-facing copy for public lead-capture forms/modals — kept in
 * one place so the wording never drifts between the /precalificacion
 * wizard, the property inquiry form, the WhatsApp intercept modal, and the
 * chat widget.
 */

/** Shown just below the submit action on every public lead-capture form/modal. */
export const PRIVACY_DISCLAIMER =
  "Al enviar sus datos, nos autoriza a utilizar esta información únicamente para conectarlo con un asesor inmobiliario. Respetamos su privacidad.";

/** Shown near the email/phone fields wherever both are visible at once, since only one of the two is required. */
export const CONTACT_METHOD_HINT = "Complete al menos uno de los dos: correo electrónico o teléfono.";
